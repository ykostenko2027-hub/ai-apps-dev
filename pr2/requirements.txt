# ПР2. Локальний inference спеціалізованої моделі

# Модель детекції (ваги завантажуються автоматично при першому запуску)
ultralytics>=8.3

# Веб-рівень
fastapi>=0.115
uvicorn[standard]>=0.30

# Приймання завантаженого файлу у FastAPI
python-multipart>=0.0.9

# Потрібен, лише якщо сторінку рендеритимете шаблоном.
# Спосіб віддати сторінку — на ваш вибір, тож рядок можна прибрати.
jinja2>=3.1
