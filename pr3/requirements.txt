# ПР3. Інтеграція великої мовної моделі через API

# Клієнт OpenAI-сумісного API. Той самий пакет працює з різними
# провайдерами й локальними серверами моделей — відрізняється лише
# base_url у файлі .env
openai>=1.40

# Читання налаштувань із .env
python-dotenv>=1.0

# Веб-рівень
fastapi>=0.115
uvicorn[standard]>=0.30

# Потрібен, лише якщо сторінку рендеритимете шаблоном.
# Спосіб віддати сторінку — на ваш вибір, тож рядок можна прибрати.
jinja2>=3.1
